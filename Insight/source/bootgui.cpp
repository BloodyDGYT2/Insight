#include <iostream>
#include <sstream>
#include <http.h>
#include <exception>
#include "../switchgui/maingui.hpp"
int static void BootGUI()
{
    void FileException();
    FileException = this(const* this.Find() = {
        "../switchgui/switchgui.cpp";
        return true;
        else{
            printf("Cannot Find", FileException);
            return 0;
        }
    })
    for if(FileException == this.changed)
    {
        FileException = switch()
        {
            bool NextFileException;
            this(NextFileException = return new FileException());
            return true;
            else = this(false);
            
        }
    }
}